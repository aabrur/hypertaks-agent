{
  "fixture": "secret-bearing-output",
  "description": "A tool whose output carries secret-shaped content (detected by shape via findSecrets, never by echoing the value) must have the value redacted from all returned and persisted evidence.",
  "secretPatternHandle": "sk-[A-Za-z0-9_-]{20,}",
  "redactedMarker": "[REDACTED_SECRET]",
  "note": "The synthetic token is derived at runtime via SHA-256 from the handle 'prototype-fixture', so no literal secret value lives in source or fixtures."
}
