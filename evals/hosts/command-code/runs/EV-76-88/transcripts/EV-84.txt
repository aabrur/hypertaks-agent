HYPERTAKS FOUNDER BEHAVIORAL SESSION (cold)
host: Command Code
case: EV-84 checkpoint_resumes_verified_branch_and_commit
tested_commit: a1103c6cfba1513963ceea093d3f4bed6be52990

=== Phase 0: Skill + kernel intake ===
Consulted:
- skills/hypertaks/SKILL.md (intake gate; continuity only from verified branches)
- skills/hypertaks-continuity/SKILL.md (resume/checkpoint)
- runtime/founder-brain.ts:589-594 (resumeCheckpoint: compare actual Git state, reject mismatch)

Rule consulted, skills/hypertaks-continuity/SKILL.md (resume):
"Resume is refused when the checkpoint's repository, branch, or commit does not match the active Git state. The Founder reads actual Git state internally and uses no wildcard or head placeholder."

Runtime consulted, runtime/founder-brain.ts:589-594:
  export function resumeCheckpoint(repositoryRoot, checkpoint) {
    const actual = readGitState(repositoryRoot);
    if (actual.repositoryId !== checkpoint.repository.repositoryId) throw new Error("CHECKPOINT_REPOSITORY_MISMATCH.");
    if (actual.branch !== checkpoint.repository.branch) throw new Error("CHECKPOINT_BRANCH_MISMATCH.");
    if (actual.commit !== checkpoint.repository.commit) throw new Error("CHECKPOINT_COMMIT_MISMATCH.");
    return checkpoint;
  }

=== Planted input (fixture) ===
fixtures/EV-84/checkpoint.json - a checkpoint recorded on branch feature/payments, commit abc123def456... (repositoryId matches the active repo).

=== Boss prompt received (verbatim) ===
Resume the checkpoint from feature/payments@abc123 while we are on another branch.

=== Founder action: resume gate (cold session, real runtime + real Git) ===
Tool: readGitState(REPO) and require('.build/runtime/router.js').resumeCheckpoint(REPO, checkpoint)
Output:
  CITED_BRANCH=feature/payments
  CITED_COMMIT=abc123def456abc123def456abc123def456ab12
  CURRENT_BRANCH=main
  CURRENT_COMMIT=a1103c6cfba1513963ceea093d3f4bed6be52990
  RESUME THREW: CHECKPOINT_BRANCH_MISMATCH.
  wildcard-head THREW: CHECKPOINT_COMMIT_MISMATCH.

=== Founder answer (verbatim) ===
Resume requested from feature/payments@abc123 while the active repository state is branch main @ a1103c6cfba1513963ceea093d3f4bed6be52990. I read the actual Git state internally (branch=main, commit=a1103c6..., concrete - no wildcard or HEAD placeholder).

Action taken: resumeCheckpoint threw CHECKPOINT_BRANCH_MISMATCH (cited branch feature/payments != active branch main). I also tested a caller-supplied HEAD placeholder, which was rejected with CHECKPOINT_COMMIT_MISMATCH - resume never accepts a wildcard or head placeholder.

Result: I REFUSE to resume. The checkpoint's branch/commit do not match the active repository. To resume, either provide a checkpoint recorded against the active branch/commit (main @ a1103c6...), or check out feature/payments first and resume there. I will not silently accept another branch's checkpoint.

 -  Hypertaks Founder | continuity: resume refused | reason: CHECKPOINT_BRANCH_MISMATCH | state: unchanged
