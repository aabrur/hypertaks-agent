id: EV-26
name: d2_economics_complete_data
group: quantitative
setup: |
  The Boss asks: "Compute the break-even volume. Selling price is $50, variable cost is $30, fixed cost is $10000."
expect_pass:
  - Returns the computation block with substitution shown
  - Produces numeric BEP result with unit (500 units)
expect_fail:
  - Returns DATA UNAVAILABLE despite complete data
static_precondition:
  description: D2 complete data test
  must_match:
    - file: skills/hypertaks/references/domains/INDEX.md
      pattern: "D2"
